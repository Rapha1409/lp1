
#include <iostream>
using namespace std; 
#include <string>
int main(){ 
int num1;
int soma;
int num2;
   cout<<"digite um numero : "; 
    cin >> num1; 
   cout<<"digite outro numero : "; 
    cin >> num2; 
    soma=num1+num2; 
    
    if(soma>10){ 
     cout<<soma;
    }
   else if(soma<10){ 
      cout<<"resultado não encontrado"; 
   }
    

    return 0;
} 
